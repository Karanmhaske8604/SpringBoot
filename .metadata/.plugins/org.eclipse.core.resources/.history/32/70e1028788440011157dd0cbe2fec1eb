package com.idempotency.web.Controller;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestHeader;
import org.springframework.web.bind.annotation.RestController;

import com.idempotency.web.Entity.Car;
import com.idempotency.web.Entity.Keys;
import com.idempotency.web.Repository.KeysRepo;
import com.idempotency.web.Service.CarService;

@RestController
public class CarController {
	
	@Autowired
	public CarService service;
	
	@Autowired
	private KeysRepo repo;
	
	@PostMapping("/add-keys")
	public Car addkeys(@RequestBody Car car, @RequestHeader(value="keys")String keys) throws Exception
	{
		try 
		{
		repo.save(new Keys(keys));
		repo.flush();
		}
		catch(DataIntegrityViolationException e)
		{
			throw new Exception("This Key has already been used ");
		}
		
		return service.add(car);
	}
	
	/*
	List<String> list=new ArrayList<String>();
	@PostMapping("/add-car")
	public Car addcar(@RequestBody Car car ,@RequestHeader(value="key") String key) throws Exception
	{
		if(list.contains(key))
		{
		 throw new Exception("Duplicate Resuest");	
		}
		list.add(key);	   return	service.add(car);	
	}*/
	
	@GetMapping("/get-car")
	public List<Car> getcar()
	{
		return service.get();
	}
	
	@DeleteMapping("/delete-by-id/{id}")
	public boolean removeid(@PathVariable(value="id")int id)
	{
		service.delete(id);
		return true;
	}

}
